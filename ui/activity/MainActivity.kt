package com.example.photos.ui.activity

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.example.photos.R

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.main_activity)

        // create recycler view
        // set layout manager
        // create view model and view model factory
        // implement observer and observe the changes in callback
        // in callback if success, set the adapter and  notify the changes
            // if failure, toast message
    }
}